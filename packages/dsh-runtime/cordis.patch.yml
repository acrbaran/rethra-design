# RethraDesign's stdio profile surface over the official dsh-base composition.
# The user's Harness installation owns credentials, settings, tools, sessions,
# and provider adapters. This bundle adds only the OD protocol boundary.

- id: system-prompt
  config:
    persona: >-
      You are a coding and design agent running for RethraDesign. Follow the
      complete task and project context supplied in the current user message.

- id: hmr
  disabled: true

- insert:
    - id: rethra-design-startup
      name: '@rethra-design/dsh-runtime/startup'

    - id: rethra-design-runtime
      name: '@rethra-design/dsh-runtime'
      inject: [rethraDesignStartup]
