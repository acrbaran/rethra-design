apiVersion: v1
kind: Service
metadata:
  name: {{ include "rethra-design.fullname" . }}
  labels:
    {{- include "rethra-design.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http-proxy
      protocol: TCP
      name: http
  selector:
    {{- include "rethra-design.selectorLabels" . | nindent 4 }}